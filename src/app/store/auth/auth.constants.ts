const path = '[auth]';

export const LOGIN_START = `${path} LOGIN START`
export const SIGN_UP_START = `${path} SIGN UP START`

export const AUTH_SUCCESS = `${path} AUTH SUCCESS`

export const AUTH_FAIL = `${path} AUTH FAIL`;

export const RESET_ERROR = `${path} RESET ERROR`

export const LOGOUT = `${path} LOGOUT`;

export const AUTO_LOGIN = `${path} AUTO LOGIN`
